内存问题已解决。现在发现了2个根本性问题：翻译前后内容混杂、注释标记重复出现。依次运行tests\parser_integration
tests\writer_integration目录的集成测试文件，查看对应的outputs目录，保证parser阶段产生的是完全干净的文本块(不包含任何需要过滤掉的信息，例如//、文档注释)，且完整记录原始块的格式信息
保证writer阶段产生的是完全没有旧内容、完全没有重复格式标记(例如// //)的写入结果。
如果outputs的可观测性存在问题(例如缺失保存的块的完整格式)，必须修改相应文件，保证一切结果可观测，并根本发现的问题作出修复

检查输出的结果是否是转换后的结果。如果是，这表明格式信息根本没有正确移除，后续翻译时也会交给翻译api，导致writer阶段格式信息必定会重复